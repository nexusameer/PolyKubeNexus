apiVersion: v1
kind: Service
metadata:
  name: devsearch-service
spec:
  type: LoadBalancer  # Change to ClusterIP or NodePort as needed
  selector:
    app: devsearch
  ports:
    - protocol: TCP
      port: 80        # External port
      targetPort: 8004  # Internal container port
